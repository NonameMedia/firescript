const inspect = require('inspect.js')
const sinon = require('sinon')
inspect.useSinon(sinon)

const  = require('')

describe('Sources', () => {
  describe('plum source', () => {
    it('', () => {

    })
  })
})
